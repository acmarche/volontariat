{% extends '@Volontariat/admin/base.html.twig' %}

{% block body %}


    <h3>Nouvelle activitée pour {{ association }}</h3>


    <div class="row">
        <div class="col-lg-8">
            {{ form(form) }}
        </div>
    </div>

    <a href="{{ path('volontariat_admin_association_show', {'id': association.id}) }}">
        <span class="fas fa-list"></span> Annuler et retour à l'association
    </a>

{% endblock %}


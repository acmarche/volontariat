{% extends '@Volontariat/admin/base.html.twig' %}

{% block body -%}

    <h3>{{ applicant }}</h3>

    <div class="row">
        <div class="col-lg-8">
            {{ form(form) }}
        </div>
    </div>

    <a href="{{ path('volontariat_admin_applicant_show', {'id':applicant.id}) }}">
        <span class="fas fa-list"></span> Annuler
    </a>

{% endblock %}

<a href="{{ path('volontariat_admin_association_edit', { 'id': association.id }) }}" role="button"
   class="btn btn-primary"><i
            class="ti ti-pencil"></i> Editer</a>

<a href="{{ path('volontariat_admin_association_password', { 'id': association.id }) }}" role="button"
   class="btn btn-primary"><i
        class="ti ti-lock"></i> Mot de passe</a>

<a href="{{ path('volontariat_admin_besoin_new', { 'id': association.id }) }}" role="button"
   class="btn btn-primary"><i
            class="ti ti-plus"></i> Ajouter une annonce</a>

<a href="{{ path('volontariat_admin_image_edit', { 'id': association.id }) }}" role="button"
   class="btn btn-primary"><i class="ti ti-image"></i> Images</a>

<a href="{{ path('volontariat_association_show', { 'slug': association.slug }) }}" role="button"
   class="btn btn-primary" target="apercu"><i class="ti ti-eye"></i> Voir sur le site</a>

<button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
    <i class="ti ti-trash"></i> Supprimer l'association
</button>

<a href="{{ path('volontariat_admin_association') }}" class="btn btn-primary mt-2" role='button'><i
            class="ti ti-list"></i>
    Retour à la Liste
</a>

<br/>
<br/>

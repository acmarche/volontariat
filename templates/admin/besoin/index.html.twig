{% extends '@Volontariat/admin/base.html.twig' %}

{% block body -%}

    <h3 class="text-success">Liste des annonces</h3>

    {% if entities | length > 0 %}
        <table class="table table-bordered table-hover">
            <thead>
            <tr>
                <th>Titre</th>
                <th>Association</th>
                <th>Date de diffusion</th>
            </tr>
            </thead>
            <tbody>
            {% for besoin in entities %}
                <tr>
                    <td>
                        <a href="{{ path('volontariat_admin_besoin_show', { 'id': besoin.id }) }}">{{ besoin }}</a>
                    </td>
                    <td>{{ besoin.association }}</td>
                    <td>
                        {% if besoin.dateBegin %}
                            Du {{ besoin.dateBegin | date('d-m-Y') }}
                        {% endif %}
                        {% if besoin.dateEnd %}
                            au  {{ besoin.dateEnd | date('d-m-Y') }}
                        {% endif %}
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    {% else %}
        <p>Aucun</p>
    {% endif %}

{% endblock %}

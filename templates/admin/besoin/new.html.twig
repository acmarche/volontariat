{% extends '@Volontariat/admin/base.html.twig' %}

{% block body %}

    <div class="card">
    <h3 class="card-header">Nouveau besoin occasionnel</h3>
    <div class="card-body">

        <div class="row">
            <div class="col-lg-8">
                {% include '@AcMarcheVolontariat/admin/besoin/_form.html.twig' %}
            </div>
        </div>
    </div>
    <div class="card-footer">
        <a href="{{ path('volontariat_admin_association_show', {'id':besoin.association.id}) }}">
            <span class="ti ti-list"></span> Annuler et retour à l'association
        </a>
    </div>
{% endblock %}
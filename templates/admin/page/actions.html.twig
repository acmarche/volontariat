<div class="hidden-print">

    <a href="{{ path('volontariat_admin_page_edit', { 'id': page.id }) }}" role="button" class="btn btn-primary"><i
                class="ti ti-pencil"></i> Editer</a>

    <a href="{{ path('volontariat_admin_page_image_edit', { 'id': page.id }) }}" role="button"
       class="btn btn-primary"><i class="ti ti-photo"></i> Images</a>

    <a href="{{ path('volontariat_page_show', { 'slug': page.slug }) }}" role="button"
       class="btn btn-primary" target="apercu"><i class="ti ti-eye"></i> Voir sur le site</a>

    <a href="{{ path('volontariat_admin_page') }}" class="btn btn-primary" role='button'><i
                class="ti ti-list"></i>
        Retour à la Liste</a>

    <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
        <i class="ti ti-trash"></i> Supprimer la page
    </button>

</div>

{% extends 'admin/base.html.twig' %}

{% block body -%}

        <h3>{{ page }}</h3>

    <div class="row">
        <div class="col-lg-8">
            {{ form(edit_form) }}
        </div>
    </div>

    <a href="{{ path('volontariat_admin_page') }}">
        <span class="fas fa-list"></span> Annuler et retour à la liste
    </a>
{% endblock %}



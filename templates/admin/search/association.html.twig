<div class="hidden-print">
    {{ form_start(form, { 'attr': {'class': 'row row-cols-lg-auto g-3 align-items-center hidden-print'}}) }}
    {{ form_errors(form) }}
    {{ form_row(form.nom, {label_attr: {class: 'sr-only'}, attr:{'class': 'my-1 mr-sm-2'} }) }}
    <br/><br/>

    <button type="submit" name="save" class="btn btn-primary my-1 mr-sm-2 mb-3">
        <i class="ti ti-device-floppy"></i> Rechercher
    </button>

    {{ form_end(form) }}
    <br/><br/>
</div>

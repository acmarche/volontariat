<a href="{{ path('volontariat_admin_user_edit', { 'id': user.id }) }}" role="button"
   class="btn btn-primary"><i
            class="ti ti-pencil"></i> Editer</a>

<a href="{{ path('volontariat_admin_user_password', { 'id': user.id }) }}" role="button"
   class="btn btn-primary"><i
            class="fas fa-lock"></i> Mot de passe</a>

<a href="{{ path('volontariat_admin_user') }}" class="btn btn-primary" role='button'><i class="ti ti-list"></i>
    Retour à la Liste</a>

<button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
    <i class="ti ti-trash"></i> Supprimer l'utilisateur
</button>
<br /><br />
{% extends "admin/base.html.twig" %}

{% block body -%}

    <h3>Valider :
        <a href="{{ path('volontariat_admin_association_show', { 'id': association.id }) }}"
           title="Voir">
            {{ association }}</a>
    </h3>

    <div class="alert alert-info">
        Un mail préviendra l'association que celle-ci a été validée.<br/>
        Un mail sera envoyé à tous les volontaires pour les prévenir de l'arrivée d'une nouvelle association
    </div>

    {{ form(form) }}

    <a href="{{ path('volontariat_admin_valider') }}">
        <span class="fas fa-list"></span> Annuler
    </a>

{% endblock %}

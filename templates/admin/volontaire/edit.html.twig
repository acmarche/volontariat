{% extends "@Volontariat/admin/base.html.twig" %}

{% block body -%}

    <div class="card">
        <h3 class="card-header">{{ volontaire }}</h3>
        <div class="card-body">
            {{ include('@Volontariat/admin/volontaire/form.html.twig') }}
        </div>
        <div class="card-footer">
            <a href="{{ path('volontariat_admin_volontaire') }}">
                <span class="ti ti-list"></span> Annuler et retour à la liste
            </a>
        </div>
    </div>
{% endblock %}

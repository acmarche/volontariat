{% if association.besoins | length > 0 %}
    <!-- Besoins -->
    <h3 class="h5 g-color-gray-dark-v1 g-mb-10">Besoins en volontariat</h3>
    <div class="g-mb-20">
        <p>De quel type d'aide l'association a besoin de manière ponctuelle pour un événement déterminé.</p>
    </div>

    <ul class="list-unstyled g-mb-12 g-mb-0--md">
        {% for besoin in association.besoins %}
            <li class="d-flex align-items-center g-mb-12">
                <i class="icon-check d-block g-color-primary g-mr-8"></i>
                <span class="d-block">
                <a href="{{ path('volontariat_besoin_show', {'id': besoin.id}) }}">
                    {{ besoin }}
                </a>
                </span>
            </li>
        {% endfor %}
    </ul>
    <hr class="g-brd-gray-light-v4">
    <!-- End besoins -->
{% endif %}

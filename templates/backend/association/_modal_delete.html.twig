<!-- Modal -->
<div class="modal fade" id="associationDeleteModal" tabindex="-1" role="dialog"
     aria-labelledby="associationDeleteModal" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-body">
                Supprimer <strong><span id="modal-body-nom"></span></strong> ?
            </div>
            <div class="modal-footer">
                {{ form_start(form_delete_association) }}
                {{ form_errors(form_delete_association) }}
                <input type="hidden" id="modal-body-input" name="id"/>
                <input type="hidden" id="associationid" name="associationid"/>
                <button type="button" class="btn btn-default" data-dismiss="modal">Annuler</button>
                <button type="submit" class="btn btn-danger">Supprimer</button>
                {{ form_end(form_delete_association) }}
            </div>
        </div>
    </div>
</div>
<!-- end Modal -->

<!-- Modal -->
<div class="modal fade" id="besoinModal" tabindex="-1" role="dialog"
     aria-labelledby="besoinModal" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-body">
                Supprimer <strong><span id="modal-body-nom"></span></strong> ?
            </div>
            <div class="modal-footer">
                {{ form_start(form_delete) }}
                <input type="hidden" id="modal-body-input" name="idbesoin"/>
                <button type="button" class="btn btn-default" data-dismiss="modal">Annuler</button>
                <button type="submit" class="btn btn-danger">Supprimer</button>
                {{ form_end(form_delete) }}
            </div>
        </div>
    </div>
</div>
<!-- end Modal -->
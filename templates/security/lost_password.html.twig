{% extends '@Volontariat/base.html.twig' %}
{% block title %}Mot de passe perdu{% endblock %}
{% block body %}
    <div class="flex min-h-full flex-col justify-center py-12 sm:px-6 lg:px-8">
        <div class="sm:mx-auto sm:w-full sm:max-w-md">
            <img class="mx-auto h-12 w-auto" src="{{ asset('/bundles/acmarchevolontariat/images/Marche.png') }}"
                 alt="Your Company">
            <h2 class="mt-6 text-center text-3xl font-bold tracking-tight text-gray-900">
                M'envoyer un lien de connection
            </h2>
                <p class="mt-6 text-lg leading-8 text-gray-600">
                    Un lien vous sera envoyé sur votre adresse courriel avec laquelle vous vous êtes inscrits.<br/>
                    Ce lien vous permettra d'être connecté à notre plate-forme.
                </p>
        </div>
        <div class="mt-8 sm:mx-auto sm:w-full sm:max-w-md">
            <div class="bg-white py-8 px-4 shadow sm:rounded-lg sm:px-10">
                {{ form_start(form, {'attr': {'class': 'space-y-6'}}) }}
                {{ form_errors(form) }}
                <div>
                    {{ form_label(form.email, null, {'label_attr': {'class': 'block text-sm font-medium leading-6 text-gray-900'}}) }}
                    {{ form_errors(form.email) }}
                    {{ form_widget(form.email, {'attr': {'class': 'block w-full rounded-md border-0 py-1.5 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 focus:ring-2 focus:ring-inset focus:ring-indigo-600 sm:text-sm sm:leading-6'}}) }}
                    {{ form_help(form.email) }}
                </div>
                <div>
                    <button type="submit"
                            class="flex w-full justify-center rounded-md bg-green-default py-2 px-3 text-sm font-semibold text-white shadow-sm hover:bg-green-light focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600">
                        Envoyer le lien sur mon adresse
                    </button>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}